import UIKit

let numberCircles = 9
let numberPlanes = 150

let widthScreen = UIScreen.main.bounds.width
let heightScreen = UIScreen.main.bounds.height
let centerX = widthScreen / 2
let centerY = heightScreen / 2
let centerScreen = CGPoint(x: centerX, y: centerY)
let maxRadius = (widthScreen - 20) / 2
